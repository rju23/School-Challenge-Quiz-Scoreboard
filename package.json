{
  "name": "schools-challenge-quiz-scoreboard",
  "version": "1.1.0",
  "description": "Desktop version of the Challenge Quiz Scoreboard for secondary-level schools",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . QuizScoreboard --platform=win32 --arch=x64 --out=dist --overwrite",
    "build": "electron-packager . QuizScoreboard --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "author": "Prakash Sejwani",
  "license": "MIT",
  "devDependencies": {
    "electron": "^26.2.0",
    "electron-packager": "^17.1.0"
  }
}
